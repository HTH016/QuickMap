<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">
	<select id="checkId" parameterType="String" resultType="int">
		select count(*) from qm where user_id=#{user_id}
	</select>
	<select id="checkNick" parameterType="String" resultType="int">
		select count(*) from qm where user_nick=#{user_nick}
	</select>
	<select id="getMember" parameterType="String" resultType="member.MemberDataBean">
		select * from qm where user_id=#{user_id}
	</select>
	<insert id="insertMember" parameterType="member.MemberDataBean">
		insert into qm values(
			#{user_id}, #{user_passwd}, #{user_name}, #{user_nick},
			#{user_tel, jdbcType=VARCHAR}, #{user_email, jdbcType=VARCHAR},
			#{user_reg_date}, #{grade_id}
		)
	</insert>
	<delete id="deleteMember" parameterType="String">
		delete from qm where user_id=#{user_id}
	</delete>
</mapper>

